{
  "name": "vite-typescript-template",
  "private": true,
  "version": "0.0.0",
  "author": "tana-gh",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && (if [ -e LICENSE ]; then cp LICENSE dist; fi) && yarn licenses list > THIRD_PARTY && cp THIRD_PARTY dist",
    "preview": "vite preview"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.8",
    "postcss-nesting": "^10.1.10",
    "postcss-normalize": "^10.0.1",
    "typescript": "^4.7.4",
    "vite": "^3.0.9"
  }
}
